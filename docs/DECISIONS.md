# 决策记录

每条记录至少说明日期、决定、原因和影响；如果决定被推翻，新增一条记录说明替代关系，不删除历史。当前项目规模较小，先集中维护本文件；日后需要独立检索时再拆分为 `docs/decisions/`。

## 2026-08-21：从零搭建并确定技术栈

- 决定：不直接复用俏皮野兽派博客模板代码，从零使用 Astro 构建。
- 技术栈：Astro、TypeScript、Tailwind CSS、Markdown / MDX、Astro Content Collections、React Islands。
- 原因：保留模板的视觉参考，同时让内容模型、卡片交互和未来扩展完全由项目自己控制。

## 2026-08-21：采用扁平条目

- 决定：不把内容固定拆成 AI、收藏、项目等顶层分区。
- 原因：同一个条目可能同时属于多个兴趣方向；类型和标签比目录树更适合持续收纳。

## 2026-08-21：首页承担探索

- 决定：探索区域直接放在首页，不单独创建探索地址。
- 交互：初始只显示部分卡片；点击卡片打开大卡片弹窗；底部使用“加载更多”。
- 原因：保持首页是唯一入口，同时避免初始内容过多造成压迫感。

## 2026-08-21：初始部署使用 GitHub Pages

- 决定：使用 GitHub 仓库保存源码，通过 GitHub Actions 构建并发布到 GitHub Pages。
- 原因：当前是纯静态内容站，部署简单、成本低，并且与 Git 驱动的内容维护方式匹配。
- 后续条件：如果出现登录、后台编辑、动态 API 或数据库需求，再评估 Cloudflare 或 Vercel。

## 2026-08-21：采用俏皮野兽派设计规范

- 决定：以 `neo-brutalist-playful` 作为项目视觉基准，从零实现，不直接复制参考模板代码。
- 核心约束：无圆角、纯黑粗边框、硬边缘阴影、鲜艳色块、轻微倾斜和明确的交互反馈。
- 原因：保留用户选择的 StyleKit 风格识别点，同时让数字花园的卡片、筛选、弹窗和内容状态共享同一套视觉语言。
- 规范位置：`docs/DESIGN.md`。

## 2026-08-21：建立分层的 AI 文档体系

- 决定：根目录 `AGENTS.md` 作为编码代理的全局权威规则；`docs/AI-WORKFLOW.md` 记录 AI 协作流程；主题文档分别维护开发、内容、设计和部署规则；`.github/copilot-instructions.md` 只做兼容入口。
- 原因：官方实践强调全局指令应短、具体、基于真实项目；路径专属规则和按需 prompt 用于隔离上下文，避免把所有内容塞进一个长文件。
- 影响：修改规则时只改权威来源；一次性任务不进入全局指令；未来目录边界稳定后再增加路径专属 instruction。
